import { buildSwaggerSpec } from 'common-js';

export const swaggerSpec = buildSwaggerSpec({
    title:       'Microservice Couchbase Ottoman.js JavaScript',
    description: 'TPC-H benchmark microservice using Ottoman.js ODM on Couchbase'
});
