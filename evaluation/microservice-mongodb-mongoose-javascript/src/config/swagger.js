import { buildSwaggerSpec } from 'common-js';

export const swaggerSpec = buildSwaggerSpec({
    title:       'Microservice MongoDB Mongoose JavaScript',
    description: 'TPC-H benchmark microservice using Mongoose ODM on MongoDB'
});
